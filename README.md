# Resource Catalog Client
A client library for interacting with the resource catalogs

## Supported Platforms
* Android
* Desktop (JVM)

## Installation
To use this library your project must be configured to use the Nexus repositories.

Add the following to your package dependencies and sync Gradle.
```
implementation 'org.bibletranslationtools:resource-catalog-client:1.0.3'
```
